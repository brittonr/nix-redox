# Minimal Redox OS

Smallest useful system — Ion shell, uutils, and a serial console.
No graphics, no networking, no user accounts.

```bash
nix run    # boots headless with serial console
nix build  # produces the disk image
```
