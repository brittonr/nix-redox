--- a/redox-rt/src/lib.rs	2026-03-27 15:36:24.211520624 -0400
+++ b/redox-rt/src/lib.rs	2026-03-27 15:36:52.700258307 -0400
@@ -295,6 +295,21 @@
 #[unsafe(no_mangle)]
 pub static mut __relibc_init_proc_fd: usize = usize::MAX;
 
+/// CWD string pointer injected by ld_so for shared libraries.
+#[used]
+#[unsafe(no_mangle)]
+pub static mut __relibc_init_cwd_ptr: usize = 0;
+
+/// CWD string length injected by ld_so for shared libraries.
+#[used]
+#[unsafe(no_mangle)]
+pub static mut __relibc_init_cwd_len: usize = 0;
+
+/// CWD fd injected by ld_so for shared libraries.
+#[used]
+#[unsafe(no_mangle)]
+pub static mut __relibc_init_cwd_fd: usize = usize::MAX;
+
 #[inline]
 pub fn current_namespace_fd() -> syscall::Result<usize> {
     // Check ld_so-injected ns_fd first (for shared libraries).
--- a/src/ld_so/linker.rs	2026-03-27 15:36:24.229099943 -0400
+++ b/src/ld_so/linker.rs	2026-03-27 15:36:52.700314342 -0400
@@ -1028,6 +1028,29 @@
                     symbol.as_ptr().cast::<usize>().write(proc_fd);
                 }
             }
+
+            // Inject CWD into the DSO so relative path resolution works.
+            if let Some((ptr_sym, _)) = obj.get_sym("__relibc_init_cwd_ptr") {
+                if let Some((len_sym, _)) = obj.get_sym("__relibc_init_cwd_len") {
+                    if let Some((fd_sym, _)) = obj.get_sym("__relibc_init_cwd_fd") {
+                        if let Some(cwd) = crate::platform::sys::path::clone_cwd() {
+                            if let Ok(cwd_guard) = crate::platform::sys::path::current_dir() {
+                                if let Some(ref cwd_obj) = *cwd_guard {
+                                    let raw_fd = cwd_obj.fd.as_raw_fd();
+                                    if let Ok(new_fd) = syscall::dup(raw_fd, b"") {
+                                        let cwd_leaked: &'static str = alloc::boxed::Box::leak(cwd);
+                                        unsafe {
+                                            ptr_sym.as_ptr().cast::<usize>().write(cwd_leaked.as_ptr() as usize);
+                                            len_sym.as_ptr().cast::<usize>().write(cwd_leaked.len());
+                                            fd_sym.as_ptr().cast::<usize>().write(new_fd);
+                                        }
+                                    }
+                                }
+                            }
+                        }
+                    }
+                }
+            }
         }
 
         obj.run_init();
--- a/src/platform/redox/path.rs	2026-03-27 15:36:24.214062062 -0400
+++ b/src/platform/redox/path.rs	2026-03-27 15:36:52.700344539 -0400
@@ -169,7 +169,13 @@
     let guard = CWD.read();
 
     if guard.as_ref().is_none() {
-        return Err(Error::new(ENOENT));
+        drop(guard);
+        get_injected_cwd();
+        let guard = CWD.read();
+        if guard.as_ref().is_none() {
+            return Err(Error::new(ENOENT));
+        }
+        return Ok(guard);
     }
 
     Ok(guard)
@@ -206,7 +212,16 @@
 pub fn canonicalize(path: &str) -> Result<String> {
     let _siglock = tmp_disable_signals();
     let cwd_guard = CWD.read();
-    canonicalize_with_cwd_internal(cwd_guard.as_ref().map(|c| c.path.as_ref()), path)
+    let cwd_path = match cwd_guard.as_ref() {
+        Some(c) => Some(alloc::string::String::from(c.path.as_ref())),
+        None => {
+            drop(cwd_guard);
+            get_injected_cwd();
+            let cwd_guard = CWD.read();
+            cwd_guard.as_ref().map(|c| alloc::string::String::from(c.path.as_ref()))
+        }
+    };
+    canonicalize_with_cwd_internal(cwd_path.as_deref(), path)
 }
 
 pub struct Cwd {
@@ -227,6 +242,25 @@
     CWD.read().as_ref().map(|cwd| cwd.path.clone())
 }
 
+/// Read CWD from ld_so-injected statics (for shared libraries).
+fn get_injected_cwd() {
+    unsafe {
+        let ptr = redox_rt::__relibc_init_cwd_ptr;
+        let len = redox_rt::__relibc_init_cwd_len;
+        let fd = redox_rt::__relibc_init_cwd_fd;
+        if ptr != 0 && len != 0 && fd != usize::MAX {
+            let bytes = core::slice::from_raw_parts(ptr as *const u8, len);
+            if let Ok(s) = core::str::from_utf8(bytes) {
+                if let Ok(dup_fd) = syscall::dup(fd, b"") {
+                    if let Ok(upper_fd) = FdGuard::new(dup_fd).to_upper() {
+                        set_cwd_manual(s.into(), upper_fd);
+                    }
+                }
+            }
+        }
+    }
+}
+
 fn open_absolute(path: &str, flags: usize) -> Result<usize> {
     if path.starts_with(libcscheme::LIBC_SCHEME) {
         libcscheme::open(path, flags)
